{
  # maximum price for an order generated by a NoiseTrader
  maxPrice = 1.0

  # maximum quantity for an order generated by a NoiseTrader
  maxQuantity = 1

  # probability that NoiseTrader generates an ask order
  askOrderProbability = 0.5
}